using UnityEngine;
using System.Collections;
public class GameClient : MonoBehaviour {

	void Awake (){ 

		NGUITools.CreateUI(false,0);
	}       

	// Use this for initialization
	void Start () {
	
	}
	
	// Update is called once per frame
	void Update () {
	
	}
}
